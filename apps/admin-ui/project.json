{
  "name": "@eshop/admin-ui",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/admin-ui/src",
  "targets": {
    "build": {
      "cache": false
    },
    "docker-build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker build -t eshop-admin-ui -f apps/admin-ui/Dockerfile ."
      }
    }
  }
}
